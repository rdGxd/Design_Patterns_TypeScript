// Neste código, é definida uma interface chamada "User" que descreve a estrutura de um objeto que representa informações de um usuário. Aqui está a definição da interface:

// Define a interface User que descreve a estrutura de um objeto de usuário
export interface User {
  name: string; // Propriedade para o nome do usuário (tipo: string)
  age: number; // Propriedade para a idade do usuário (tipo: number)
}

/*
Nesta interface, são especificadas duas propriedades: name e age, ambas com tipos definidos. A propriedade name é do tipo string e representa o nome do usuário, enquanto a propriedade age é do tipo number e representa a idade do usuário. Essa interface pode ser usada para definir a estrutura esperada de objetos que representam usuários em várias partes do código.
*/
