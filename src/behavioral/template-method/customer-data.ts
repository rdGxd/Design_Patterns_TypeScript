export type CustomerData = {
  // Define um objeto que representa um cliente
  name: string;
  age: string;
  cpf: string;
};

/*
A primeira linha comenta que o tipo CustomerData define um objeto que representa um cliente.
A segunda linha comenta que a propriedade name deve ser uma string que representa o nome do cliente.
A terceira linha comenta que a propriedade age deve ser uma string que representa a idade do cliente.
A quarta linha comenta que a propriedade cpf deve ser uma string que representa o CPF do cliente.
*/
