// Definindo a interface ECommerceProductProtocol
export interface ECommerceProductProtocol {
  name: string; // Propriedade representando o nome do produto
  price: number; // Propriedade representando o preço do produto
}

/*
Foi definida a interface ECommerceProductProtocol.

A interface possui duas propriedades:

name: Representa o nome do produto.
price: Representa o preço do produto.
*/
